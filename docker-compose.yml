version: '3.8'

services:
  imaginary-web:
    build:
      context: .
      dockerfile: src/Imaginary.Web/Dockerfile
    container_name: imaginary-web
    ports:
      - "8080:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
    restart: unless-stopped
